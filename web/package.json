{
  "name": "marp-slides-web",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "npm run build:slides && vite",
    "build": "npm run build:slides && tsc --noEmit && vite build",
    "build:slides": "node scripts/build-slides.mjs",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@marp-team/marp-cli": "^4.1.2",
    "typescript": "^5.6.3",
    "vite": "^5.4.10"
  }
}
